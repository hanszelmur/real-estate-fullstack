/**
 * Customer Frontend Configuration
 * 
 * Contains API endpoint and other configuration settings.
 */

const CONFIG = {
    // Backend API URL - change this if backend runs on different port/host
    API_URL: 'http://localhost:3000/api',
    
    // Local storage keys
    STORAGE_KEYS: {
        TOKEN: 'real_estate_token',
        USER: 'real_estate_user',
        PENDING_PHONE: 'real_estate_pending_phone'
    }
};
